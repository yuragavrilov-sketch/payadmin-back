# prod — деплой в PROD.
# Вся конфигурация приложения (spring.datasource.*, spring.security.oauth2.*)
# приходит из Vault через config-server по пути pay/app/payadmin/prod.
# Ключи в Vault должны быть в формате spring-свойств (см. README -> "Что положить в Vault").
